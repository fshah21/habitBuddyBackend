import { Request, Response } from "express";
import { db } from '../index.js';
import { v4 as uuidv4 } from 'uuid';

export class MatchController {
    static async matchUserGoal(req: Request, res: Response) {
        
    }
}